# AGENTS.md - 開発エージェント向けガイドライン

本ファイルは、このプロジェクトで機能追加やリファクタリングを行うAIエージェント（Antigravity等）のための開発ガイドラインおよび設計思想をまとめたものです。

## プロジェクトの概要
ポケモンチャンピオンズ向けのデータ分析Webアプリケーションの開発。

## エージェントへの指示と開発フロー
1. **計画の遵守**: 
   - 開発や設計を行う際は、必ず `plan/` ディレクトリ配下にある連番付きの計画ファイル（例: `01_xxx.md`）を参照し、それに従って進めてください。
   - **【重要】全体の計画がユーザーと合意され、まとまるまでは実装（コマンドの実行やコードの記述）を行わないでください。** 計画の段階では、勝手にプロジェクトの初期化（npm create等）やパッケージのインストールを行わず、設計案の議論と計画書の作成に専念してください。
   - 新しい実装ステップに進む前には、計画を更新または新たな計画ファイルを作成し、全体の開発ロードマップを整理してください。
2. **ローカルファースト / WASM**:
   - 画像解析などの処理は、サーバーサイドではなく、ブラウザ側（WebAssembly）で実行する構成とします。
   - プライバシー保護およびサーバーコスト削減のため、ローカルでの計算・推論を優先します。
3. **コード品質と設計**:
   - Webアプリケーション開発のルールに従い、リッチでプレミアムなデザイン（CSS、HSLカラー、スムーズなアニメーション等）を意識してください。
   - プレースホルダー画像は使用せず、必要に応じて `generate_image` を使用します。
4. **TDD (テスト駆動開発) の遵守**:
   - 機能実装を行う際は、必ず **TDD (テスト駆動開発)** を遵守してください。
   - 具体的には、新しい機能やロジック（ステータス計算、分析エンジン、画像処理等）を追加・変更する前にまずテストコードを作成し（Red）、それをパスする実装を行い（Green）、その後コードのクリーンアップ（Refactor）を行うサイクルを徹底してください。

## 共通コンポーネントの設計方針 (UI抽象化)
- **場所**: `src/components/common/`
- **対象**: ボタン (`Button.tsx`)、ドロップダウン (`Select.tsx`)、テキスト入力 (`Input.tsx`)、チェックボックス (`Checkbox.tsx`)、タイプバッジ (`TypeBadge.tsx`)、しぼりこみ入力 (`Autocomplete.tsx`) などの再利用可能なUI部品。
- **方針**:
  - 重複したスタイリングや素の HTML 要素を個別の画面（`PartySimulator.tsx` や `StatSearch.tsx` など）に直接記述するのではなく、`src/components/common` の共通コンポーネントを利用して構築します。
  - 新たな共通UIコンポーネントを追加・変更する場合は、プロジェクト全体の **TDD (テスト駆動開発)** 方針に従い、必ず単体テストコードを先に作成してから実装を行ってください。

## 共通データの設計方針 (データ共通化)
- **場所**: `src/utils/pokemon.ts`
- **対象**: タイプ一覧 (`TYPES`)、タイプ翻訳 (`typeTranslations`)、タイプカラー (`typeColors`)、メガストーンの対応関係 (`megaStoneMap`) などのポケモンに関する共通データ・定数。
- **方針**:
  - 複数箇所で同じ定数や翻訳マッピングを定義することを避け、必ず `src/utils/pokemon.ts` の共通定義からインポートして使用してください。
  - コンポーネントをスリム化するため、定義行数が多いマッピングデータ（メガストーンなど）も共通定義に集約します。

